from django.newforms import form_for_model, form_for_instance
from specs.members.forms import MemberForm
from specs.members.models import Member

    call = request.GET.get('call', '')

    if call == 'update':
        cur_member = Member.objects.get(id=object_id) 
        UpdateForm = form_for_instance(cur_member)        
        form = UpdateForm()
    else:
        form = MemberForm()