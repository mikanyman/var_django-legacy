from django.contrib import admin
from specs.frontpg.models import Article

class ArticleAdmin(admin.ModelAdmin):
    list_display = ('headline', 'author')
    
#admin.site.register(Article, ArticleAdmin)